<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="20dp"
    android:layout_marginVertical="5dp"
    app:cardBackgroundColor="#00000000"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_glass_card"
        android:paddingHorizontal="16dp"
        android:paddingVertical="14dp">

        <!-- Avatar Inisial -->
        <FrameLayout
            android:id="@+id/avatarContainerBayar"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:background="@drawable/bg_avatar">

            <TextView
                android:id="@+id/tvAvatarInisialBayar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="A"
                android:textColor="#2563EB"
                android:textSize="16sp"
                android:fontFamily="sans-serif-black" />
        </FrameLayout>

        <!-- Info Anggota -->
        <LinearLayout
            android:id="@+id/infoContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginStart="12dp"
            android:layout_toEndOf="@+id/avatarContainerBayar"
            android:layout_toStartOf="@+id/btnBayar"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvNamaAnggotaBayar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Nama Anggota"
                android:textColor="@color/text_main"
                android:textSize="14sp"
                android:fontFamily="sans-serif-bold"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/tvIdAnggotaBayar"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="3dp"
                android:alpha="0.45"
                android:text="ID: 001"
                android:textColor="@color/text_main"
                android:textSize="12sp" />
        </LinearLayout>

        <!-- Tombol Bayar -->
        <Button
            android:id="@+id/btnBayar"
            android:layout_width="80dp"
            android:layout_height="36dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:backgroundTint="@color/brand_primary"
            android:text="Bayar"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:fontFamily="sans-serif-bold"
            app:cornerRadius="12dp" />
    </RelativeLayout>
</androidx.cardview.widget.CardView>